name: randomtable2026-env

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python=3.13
  - pytest
  - blackd
  - isort

  # remove/modify these as needed
  - numpy
  - pandas
  - jupyter
  # keep this to install your Python package locally
  - pip
  - pip:
    - --editable .
